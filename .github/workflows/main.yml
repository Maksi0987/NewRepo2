name: Build and Push to Docker Hub

on:
  push:
    branches: [ "master", "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: arentumr
          password: dckr_pat_nQ86vCkg-_MROIr_QQscUWG2D0E

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./WebApplication9/Dockerfile
          push: true
          tags: |
            arentumr/neurohub:v${{ github.run_number }}
            arentumr/neurohub:latest

      - name: Send Telegram Notification
        if: success()
        uses: appleboy/telegram-action@master
        with:
          to: 1328874413
          token: 8506509386:AAHApa6IwiSICT6i0vB0c-ACTvOGV0M--7c
          message: |
             Збірка NeuroHub №${{ github.run_number }} успішна!
             Образ: arentumr/neurohub:latest
             Комміт: ${{ github.event.head_commit.message }}
